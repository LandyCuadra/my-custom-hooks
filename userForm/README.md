#useForm

use example:
```
    const intialForm = {
        name: '',
        age: 30,
        email: 'orlandocuadra350@hotmail.com'
    };
    const [formValues, handleInputChange, reset] = useForm(intialForm);
```
